\begin{algorithm}[t]
  \SetKwInOut{Input}{input}
  \SetKwInOut{Output}{output}
    
  \caption{Iterative Fibbonacci}
    
  \Input{A natural number $n$}
  \Output{The $n$-th Fibbonacci number}
    
  \BlankLine
  %Let $\lambda$ be the source-sink edge connectivity of graph $G$.\\
  %Assign $F(e) \leftarrow 0$ for all $e \in E$.\\
  %\For{$h \leftarrow \lambda$ \KwTo $1$}{
    %Find the $h$-route flow of graph $G$.\\
    %Let $F_h: E \rightarrow \mathbb{R}^+$ be the flow on each edge in that $h$-route flow.\\
    %$F(e) = F(e) + F_h(e).$\\
    %$c(e) = c(e) - F_h(e).$\\
  }
  \label{algo_iterfib}
\end{algorithm}